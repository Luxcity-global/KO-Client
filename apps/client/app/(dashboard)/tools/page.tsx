import { redirect } from 'next/navigation';

/** Mortgage Tools archived — calculators live in components/calculators/ for re-enable. */
export default function ToolsPage() {
  redirect('/overview');
}
